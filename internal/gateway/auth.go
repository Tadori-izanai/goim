package gateway

import "context"

func (g *Gateway) Register(c context.Context, username, password string) (any, error) {
	// todo
	return nil, nil
}

func (g *Gateway) Login(c context.Context, username, password string) (any, error) {
	// todo
	return nil, nil
}
